# 📚 Аналог Obsidian Publish для GitHub Pages

Статический сайт для публикации лекций в формате Markdown на GitHub Pages. Студенты могут читать лекции прямо в браузере с удобной навигацией по файлам.

## ✨ Возможности

- 📖 Просмотр Markdown файлов с красивым форматированием
- 📁 Навигация по файловой структуре с поддержкой папок
- 🔍 Поиск по названиям и содержимому файлов
- 🌙 Темная/светлая тема с сохранением выбора
- 💻 Подсветка синтаксиса кода (highlight.js)
- 📋 Кнопки копирования кода одной кнопкой
- 📑 Автоматическая таблица содержания (TOC)
- 🖨️ Печать документов
- 📱 Адаптивный дизайн для мобильных устройств
- 🚀 Автоматический деплой на GitHub Pages
- 🌐 Работает полностью статически (без сервера)

## 🚀 Быстрый старт

### 1. Клонирование и настройка

```bash
git clone <ваш-репозиторий>
cd analog-obsidian
```

### 2. Добавление лекций

Поместите ваши Markdown файлы в папку `lectures/`. Вы можете создавать подпапки для организации:

```
lectures/
  ├── Лекция-1-Введение.md
  ├── Лекция-2-Основы.md
  └── Раздел-2/
      ├── Лекция-3-Продвинутые-темы.md
      └── Лекция-4-Практика.md
```

### 3. Генерация индекса

Перед коммитом запустите скрипт для генерации индекса файлов:

```bash
node generate-index.js
```

Этот скрипт создаст файл `lectures/index.json` со списком всех Markdown файлов.

### 4. Настройка GitHub Pages

**ВАЖНО:** Перед первым деплоем нужно настроить GitHub Pages вручную:

1. Перейдите в настройки репозитория на GitHub (`Settings`)
2. В левом меню найдите раздел **"Pages"** (в разделе "Code and automation")
3. В разделе **"Source"** выберите **"GitHub Actions"**
4. Сохраните изменения (нажмите "Save")

**Если возникает ошибка "Get Pages site failed":**
- Убедитесь, что вы настроили Pages вручную (шаги выше)
- Проверьте, что у вас есть права администратора на репозиторий
- Для приватных репозиториев нужен GitHub Pro/Team/Enterprise
- После первой настройки Pages вручную, workflow будет работать автоматически

### 5. Деплой

Просто закоммитьте и запушьте изменения:

```bash
git add .
git commit -m "Добавлены лекции"
git push origin main
```

GitHub Actions автоматически задеплоит сайт на GitHub Pages. Через несколько минут ваш сайт будет доступен по адресу:

```
https://<ваш-username>.github.io/<название-репозитория>/
```

## 📝 Формат лекций

Используйте стандартный Markdown формат. Поддерживаются:

- Заголовки всех уровней (автоматически создается таблица содержания)
- **Жирный** и *курсивный* текст
- Списки (нумерованные и маркированные)
- Блоки кода с подсветкой синтаксиса (поддерживаются все популярные языки)
- Таблицы
- Цитаты
- Ссылки и изображения
- Горизонтальные линии

Примеры лекций находятся в папке `lectures/`.

## 🎯 Новые функции

### Темная тема
- Переключатель темы в верхней части боковой панели
- Выбор темы сохраняется в браузере
- Автоматическая адаптация подсветки кода под тему

### Поиск
- **По названиям**: быстрый поиск по названиям файлов (по умолчанию)
- **По содержимому**: включите чекбокс "Искать в содержимом" для поиска внутри файлов
- Результаты подсвечиваются автоматически

### Таблица содержания
- Нажмите кнопку 📑 в заголовке для отображения TOC
- Автоматически генерируется из заголовков документа
- Клик по пункту TOC плавно прокручивает к нужному разделу
- Активный раздел подсвечивается при прокрутке

### Подсветка кода
- Автоматическое определение языка программирования
- Поддержка всех популярных языков
- Кнопка "Копировать" появляется при наведении на блок кода
- Подсветка адаптируется под выбранную тему

### Мобильная версия
- Адаптивный дизайн для всех размеров экранов
- Выдвижное меню навигации на мобильных устройствах
- Оптимизированная таблица содержания для маленьких экранов

## 🛠 Локальная разработка

Для локального тестирования вы можете использовать любой простой HTTP сервер:

### Python 3

```bash
python3 -m http.server 8000
```

### Node.js (http-server)

```bash
npx http-server -p 8000
```

Затем откройте в браузере: `http://localhost:8000`

## 📁 Структура проекта

```
.
├── index.html          # Главная страница
├── styles.css          # Стили
├── app.js              # JavaScript логика
├── generate-index.js   # Скрипт генерации индекса
├── lectures/           # Папка с лекциями
│   ├── index.json      # Автоматически генерируемый индекс
│   └── *.md            # Ваши Markdown файлы
├── .github/
│   └── workflows/
│       └── deploy.yml  # GitHub Actions workflow
└── README.md           # Этот файл
```

## 🎨 Кастомизация

### Изменение цветовой схемы

Отредактируйте переменные CSS в файле `styles.css`:

```css
:root {
    --bg-primary: #ffffff;
    --bg-sidebar: #2d2d2d;
    --accent: #4a9eff;
    /* ... и т.д. */
}
```

### Изменение пути к лекциям

В файле `app.js` измените константу:

```javascript
const LECTURES_PATH = 'lectures'; // Измените на ваш путь
```

## 🔧 Требования

- Node.js (для генерации индекса, опционально)
- GitHub аккаунт (для хостинга)

## 📄 Лицензия

Используйте свободно для своих проектов!

## 🤝 Вклад

Если у вас есть идеи по улучшению, создавайте Issues и Pull Requests!

---

**Приятного использования!** 🎓
